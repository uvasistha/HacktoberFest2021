package LudoPack;

import java.util.ArrayList;
import java.util.Arrays;

public class Config {
    private static final ArrayList<EntryExitPoint> entryExitpoints = new ArrayList<>(Arrays.asList(
            /*Add all the entry exit points for the 4 colors*/
    ));
    private static final ArrayList<Integer> setPoints = new ArrayList<>(Arrays.asList(
            /*Add the safe points for the 4 colors*/
    ));

}
